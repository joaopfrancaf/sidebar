import { createStitches } from '@stitches/react';

export const {
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
  config,
} = createStitches({
  theme: {
    colors: {
      bodybgcolor: '#dce4e3',
      green: '#18c29c',
      lightgreen: '#8ed7c6',
	    lightgrey: '#dce4e3',
      textcolor: '#084236',
    },
  },
});